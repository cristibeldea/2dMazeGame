screenWidth = 800
screenHeight = 600
tileSize = 50

widthTiles = screenWidth // tileSize
heightTiles = screenHeight // tileSize

screenCenter = ((screenWidth - tileSize)/2, (screenHeight-tileSize)/2)
mapSize = 60

keyPosition = [45, 41]
doorPosition = [30, 8]

FPS = 60
step = tileSize // 5